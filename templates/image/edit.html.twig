{% extends 'base.html.twig' %}

{% block title %}Editar Imagem{% endblock %}

{% block body %}
    <h1 class="text-center p-4 mt-3">Editar Imagem</h1>

    {{ include('image/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('image_index') }}" class="btn btn-secondary mt-2">Voltar para listagem</a>

    {{ include('image/_delete_form.html.twig') }}
{% endblock %}
